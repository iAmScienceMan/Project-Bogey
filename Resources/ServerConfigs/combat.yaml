id: combat
name: Combat Test
scenario: combat-test
seed: 42
tickRate: 15
port: 8712
maxPlayers: 4

# Advertise this server to the hub so it appears in the client's browser.
# Flip advertise to true, point url at your hub, and set address to the
# host:port clients actually connect to (your public ip for real hosting).
hub:
  advertise: true
  url: http://localhost:8080/
  address: 127.0.0.1:8712
